'use babel';

import ChristopherWordCount2View from '../lib/christopher-word-count2-view';

describe('ChristopherWordCount2View', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
